export interface Video {
  thumbnail: string;
  title: string;
  channel: string;
  views: string;
  time: string;
} 

export interface VideoCardProps {
  thumbnail: string;
  title: string;
  channel: string;
  views: string;
  time: string;
}
